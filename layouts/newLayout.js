/*
  TODO: your cool layout here!
*/

async function newLayout (setOfSets) {
  // TODO: change the positions of each of setOfSets.elements,
  // and optionally the order of setOfSets.sets' members lists

  return setOfSets;
}
// This label will show up in the interface
newLayout.interfaceLabel = 'Custom layout';

export default newLayout;
